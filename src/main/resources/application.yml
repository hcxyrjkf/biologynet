spring:
  profiles:
    active: dev

  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/biologynet
    username: root
    password:
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: update
    show-sql: true
